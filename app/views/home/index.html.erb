<% Category.all.each do |cat| %>
  <h2><%= cat.name %></h2>

  <table id="<%= cat.technical_name %>">
    <tbody>
      <% Inventory.where(:category_id => cat.id).each do |i| %>
        <tr>
          <td>
            <%= i.data %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>
